# Chapter 11 — brief for Claude Code

> Chapter 11: MLOps and LLMOps

- **Stage:** Operations (accent `#7B61E8`) — use `stageForChapter(11)` / `accentForChapter(11)`.
- **PDF pages:** 430–489  (**60 pages**, 36 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter11.tsx` exporting `const CHAPTER11: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 11.1 | 433 | `data/figures/p433.png` | Figure 11.1: DevOps lifecycle steps |
| 11.2 | 435 | `data/figures/p435.png` | Figure 11.2: Relationship between data, model, and code changes |
| 11.3 | 438 | `data/figures/p438.png` | Figure 11.3: DS vs. MLE vs. MLOps |
| 11.4 | 443 | `data/figures/p443.png` | Figure 11.4: Example trace in the Langfuse UI |
| 11.5 | 446 | `data/figures/p446.png` | Figure 11.5: Infrastructure flow |
| 11.6 | 449 | `data/figures/p449.png` | Figure 11.6: Qdrant cluster dashboard example after being populated with two collections. |
| 11.7 | 456 | `data/figures/p456.png` | Figure 11.7: AWS ECR example |
| 11.8 | 456 | `data/figures/p456.png` | Figure 11.8. Also, you can copy the ECR URL to fill the AWS_ECR_URL variable from the main AWS |
| 11.9 | 457 | `data/figures/p457.png` | Figure 11.9: AWS ECR repository example after the Docker image is pushed |
| 11.10 | 458 | `data/figures/p458.png` | the ZenML dashboard, you can visualize the latest state of the pipeline, as seen in Figure 11.10. |
| 11.11 | 459 | `data/figures/p459.png` | step, and the logs for monitoring and debugging, as illustrated in Figure 11.11: |
| 11.12 | 460 | `data/figures/p460.png` | section, as shown in Figure 11.12. |
| 11.13 | 462 | `data/figures/p462.png` | Figure 11.13: SageMaker—ml.t3.medium expected quotas |
| 11.14 | 464 | `data/figures/p464.png` | Figure 11.14: CI/CD pipelines flow |
| 11.15 | 470 | `data/figures/p470.png` | Figure 11.15: GitHub Actions CI pipeline run example |
| 11.16 | 473 | `data/figures/p473.png` | Figure 11.16: GitHub Actions CD pipeline run example |
| 11.17 | 474 | `data/figures/p474.png` | Figure 11.17: Configuring only repository name |
| 11.18 | 475 | `data/figures/p475.png` | Figure 11.18: GitHub Actions secrets |
| 11.19 | 476 | `data/figures/p476.png` | Figure 11.19: CT pipeline |
| 11.20 | 479 | `data/figures/p479.png` | pipeline (which we want to avoid), as illustrated in Figure 11.20. Usually, you want to keep each |
| 11.21 | 483 | `data/figures/p483.png` | Figure 11.21: Inference pipeline serving architecture |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Python`, `Poetry`, `ZenML`, `Hugging Face`, `Comet ML`, `Comet`, `Opik`, `MongoDB`, `Qdrant`, `AWS`, `SageMaker`, `Docker`, `GitHub Actions`, `GitHub`, `FastAPI`, `OpenAI`, `Llama`, `Terraform`, `Qwak`, `GitHub Copilot`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.431 — The path to LLMOps: Understanding its roots in DevOps and MLOps
  - p.432 — DevOps
    - p.432 — The DevOps lifecycle
    - p.433 — The core DevOps concepts
  - p.434 — MLOps
    - p.436 — MLOps core components
    - p.436 — MLOps principles
    - p.438 — ML vs. MLOps engineering
  - p.439 — LLMOps
    - p.440 — Human feedback
    - p.440 — Guardrails
    - p.442 — Prompt monitoring
- p.444 — Deploying the LLM Twin’s pipelines to the cloud
  - p.445 — Understanding the infrastructure
  - p.447 — Setting up MongoDB
  - p.448 — Setting up Qdrant
  - p.450 — Setting up the ZenML cloud
    - p.453 — Containerize the code using Docker
    - p.457 — Run the pipelines on AWS
    - p.461 — Troubleshooting the ResourceLimitExceeded error after running a ZenML pipeline on SageMaker
- p.463 — Adding LLMOps to the LLM Twin
  - p.463 — LLM Twin’s CI/CD pipeline flow
    - p.465 — More on formatting errors
    - p.465 — More on linting errors
  - p.466 — Quick overview of GitHub Actions
  - p.467 — The CI pipeline
    - p.467 — GitHub Actions CI YAML file
  - p.471 — The CD pipeline
  - p.474 — Test out the CI/CD pipeline
  - p.475 — The CT pipeline
    - p.477 — Initial triggers
    - p.478 — Trigger downstream pipelines
  - p.480 — Prompt monitoring
  - p.486 — Alerting
- p.487 — Summary
- p.488 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 430 |  Chapter 11: MLOps and LLMOps | _____ | MLOps and LLMOps Throughout the book, we’ve already used machine learning operations (MLOps) components and principles such as a model registry to sha |
| 431 |  The path to LLMOps: Understanding its ro | _____ | On the implementation side of things, to add LLMOps to our LLM Twin use case, we will deploy all our ZenML pipelines to AWS. We will implement a conti |
| 432 |  The DevOps lifecycle | _____ | DevOps Manually shipping software is time-consuming, error-prone, involves security risks, and doesn’t scale. Thus, DevOps was born to automate the pr |
| 433 |  The core DevOps concepts | _____ | 3. Build: Package your applications and dependencies into an executable format. 4. Test: This stage is crucial. It’s where you confirm that your code  |
| 434 |  MLOps | _____ | • Version control: Used to track, manage, and version every change made to the source code. This allows you to have complete control over the evolutio |
| 435 |  MLOps | _____ | In ML systems, a build can be triggered by any change in these areas—whether it’s an update in the code, modifications in the data, or adjustments to  |
| 436 |  MLOps principles | _____ | • After serving the model in the production environment and collecting feedback from the end users, we might recognize that the assumptions we made fo |
| 437 |  MLOps principles | _____ | They are: • Automation or operationalization: Automation in MLOps involves transitioning from manual processes to automated pipelines through CT and C |
| 438 |  ML vs. MLOps engineering | _____ | ML vs. MLOps engineering There is a fine line between ML engineering and MLOps. If we want to define a rigid job description for the two rules, it can |
| 439 |  LLMOps | _____ | The ML engineer takes the functional models from the DS team and constructs a layer on top of them, making them modular and extendable and providing a |
| 440 |  Guardrails | _____ | • Managing massive datasets and multi-GPU clusters involves substantial costs. For ex- ample, the estimated training cost for GPT-4 is around $100 mil |
| 441 |  Guardrails | _____ | Most users have accepted this phenomenon, but what is not acceptable is when LLMs accidentally output sensitive information, such as GitHub Copilot ou |
| 442 |  Prompt monitoring | _____ | • Output guardrails: At the output of an LLM response, you want to catch failed outputs that don’t respect your application’s standards. This can vary |
| 443 |  Prompt monitoring | _____ | Also, tracking the total input and output tokens is critical to understanding the costs of hosting your LLMs. Ultimately, you can compute metrics that |
| 444 |  Deploying the LLM Twin’s pipelines to th | _____ | As shown in Figure 11.4, the end goal is to trace each step from the user’s input until the generated answer. If something fails or behaves unexpected |
| 445 |  Understanding the infrastructure | _____ | Understanding the infrastructure Before diving into the step-by-step tutorial, where we will show you how to set up all the nec- essary components, le |
| 446 |  Understanding the infrastructure | _____ | Figure 11.5: Infrastructure flow Now that we understand what the essential resources of our infrastructure are, let’s look over the core flow of runni |
| 447 |  Setting up MongoDB | _____ | 3. Now, we can trigger any pipeline implemented during this book either from the CLI of our local machine or ZenML’s dashboard. 4. Each step from ZenM |
| 448 |  Setting up Qdrant | _____ | d. Choose Frankfurt (eu-central-1) as your region. You can choose another region, but be careful to choose the same region for all future AWS services |
| 449 |  Setting up Qdrant | _____ | 3. Fill out the cluster creation form with the following: a. Choose the Free version of the cluster. b. Choose GCP as the cloud provider (while writin |
| 450 |  Setting up the ZenML cloud | _____ | That’s it! Instead of reading and writing from your local Qdrant vector DB, you will do it from the cloud Qdrant cluster we just created. Just to be s |
| 451 |  Setting up the ZenML cloud | _____ | To ship the code to AWS, you must create a ZenML stack. A stack is a set of components, such as the underlying orchestrator, object storage, and conta |
| 452 |  Setting up the ZenML cloud | _____ | 8. Click the Deploy to AWS button. It will open a CloudFormation page on AWS. ZenML leverages CloudFormation (an infrastructure as code, or IaC, tool) |
| 453 |  Containerize the code using Docker | _____ | Containerize the code using Docker So far, we have defined our infrastructure, MongoDB, Qdrant, and AWS, for storage and computing. The last step is t |
| 454 |  Containerize the code using Docker | _____ | echo "deb [signed-by=/usr/share/keyrings/google-linux-signing-key.gpg] https://dl.google.com/linux/chrome/deb/ stable main" > /etc/apt/sources. list.d |
| 455 |  Containerize the code using Docker | _____ | Additionally, the poethepoet plugin is installed to help manage tasks within the project. Finally, any remaining Poetry cache is removed to keep the c |
| 456 |  Containerize the code using Docker | _____ | Figure 11.7: AWS ECR example The first step is to authenticate to ECR. For this to work, ensure that you have the AWS CLI installed and configured wit |
| 457 |  Run the pipelines on AWS | _____ | Now we have to add another tag to the llmtwin Docker image that signals the Docker registry we want to push it to: docker tag llmtwin ${AWS_ECR_URL}:l |
| 458 |  Run the pipelines on AWS | _____ | Return to your AWS ECR ZenML repository and copy the image URI as shown in Figure 11.9. Then, go to the configs directory, open the configs/end_to_end |
| 459 |  Run the pipelines on AWS | _____ | In the Adding LLMOps to the LLM Twin section, we will explain why we compressed all the steps into a single pipeline. Figure 11.10: ZenML example of r |
| 460 |  Run the pipelines on AWS | _____ | Figure 11.11: ZenML step metadata example To find even more details about the runs, you can go to AWS SageMaker. In the left panel, click SageMaker da |
| 461 |  Troubleshooting the ResourceLimitExceede | _____ | This will open a list of all the processing jobs that execute your ZenML pipelines. Figure 11.12: SageMaker dashboard Troubleshooting the ResourceLimi |
| 462 |  Troubleshooting the ResourceLimitExceede | _____ | Then, in the left panel, click on AWS services, search for Amazon SageMaker, and then for ml.t3. medium. In Figure 11.13, you can see our quotas for t |
| 463 |  LLM Twin’s CI/CD pipeline flow | _____ | Adding LLMOps to the LLM Twin In the previous section, we saw how to set up the infrastructure for the LLM Twin project by manually building the Docke |
| 464 |  LLM Twin’s CI/CD pipeline flow | _____ | If any check fails, the CI pipeline fails, and the developer who created the PR cannot merge it into the staging branch until it fixes the issues. Imp |
| 465 |  More on linting errors | _____ | More on formatting errors Formatting errors relate to the style and structure of your code, ensuring that it adheres to a consistent visual layout. Th |
| 466 |  Quick overview of GitHub Actions | _____ | Quick overview of GitHub Actions GitHub Actions is a CI/CD platform provided by GitHub that allows developers to automate their workflows directly wit |
| 467 |  GitHub Actions CI YAML file | _____ | The workflows are triggered by events like push, pull_request, or schedule. For example, you might trigger a workflow every time code is pushed to a s |
| 468 |  GitHub Actions CI YAML file | _____ | The first job, named QA, is responsible for quality assurance tasks like code checks and format- ting verification. Within the qa job, the first step  |
| 469 |  GitHub Actions CI YAML file | _____ | The qa job then runs several quality checks on the code. The first check uses a tool called gitleaks to scan for secrets in the codebase, ensuring tha |
| 470 |  GitHub Actions CI YAML file | _____ | poetry install –-without aws poetry self add 'poethepoet[poetry_plugin]' Finally, the test job runs the project’s tests using the poetry poe test comm |
| 471 |  The CD pipeline | _____ | The CD pipeline The CD pipeline will automate the Docker steps we manually performed in the Deploying the LLM Twin’s pipelines to the cloud section, w |
| 472 |  The CD pipeline | _____ | The next step involves configuring the AWS credentials. This step is crucial for interacting with AWS services, such as Amazon Elastic Container Regis |
| 473 |  The CD pipeline | _____ | ${{ steps.login-ecr.outputs.registry }}/${{ secrets.AWS_ECR_NAME }}:latest push: true To conclude, the CD pipeline authenticates to AWS, builds the Do |
| 474 |  Test out the CI/CD pipeline | _____ | The last step in setting up the CI/CD pipeline is to test it and see how it works. Test out the CI/CD pipeline To test the CI/CD pipelines yourself, y |
| 475 |  The CT pipeline | _____ | After the CD GitHub Actions are complete, check the ECR repository to see whether the Docker image was pushed successfully. Figure 11.18: GitHub Actio |
| 476 |  The CT pipeline | _____ | • The FTI architecture: A modular system with clear interfaces and components made it easy to capture the relationship between the pipelines and autom |
| 477 |  Initial triggers | _____ | For the LLM Twin’s CT pipeline, we have to discuss the initial trigger that starts the pipelines and how the pipelines are triggered by each other. In |
| 478 |  Trigger downstream pipelines | _____ | The conclusion is that once you can manually trigger all your ML pipelines through a single command, you can quickly adapt it to more advanced and com |
| 479 |  Trigger downstream pipelines | _____ | To keep the function light, we added all the logic up to computing the features. But, as we sug- gested in the code snippet above, you can easily add  |
| 480 |  Prompt monitoring | _____ | Unfortunately, the ZenML cloud’s free trial has a limitation of a maximum of three pipelines. As we have more, we avoided that limitation by compressi |
| 481 |  Prompt monitoring | _____ | We want to log the entire trace from the user’s input until the final result is available. Before diving into the LLM Twin use case, let’s look at a s |
| 482 |  Prompt monitoring | _____ | Then, you have preprocessing and postprocessing functions surrounding the actual LLM call. Using the @track() decorator, we log the input and output o |
| 483 |  Prompt monitoring | _____ | You can expand on this idea and log various feedback scores. The most common is asking the user if the generated answer is valuable and correct. Anoth |
| 484 |  Prompt monitoring | _____ | As our implementation is already modular, using Opik makes it straightforward to log an end- to-end trace of a user’s request: from opik import track  |
| 485 |  Prompt monitoring | _____ | expand_to_n_queries: int = 3, ) -> list: query_model = Query.from_str(query) query_model = self._metadata_extractor.generate(query_model) … # Rest of  |
| 486 |  Alerting | _____ | "temperature": settings.TEMPERATURE_INFERENCE, "prompt_tokens": compute_num_tokens(prompt), "total_tokens": compute_num_tokens(answer), } ) return ans |
| 487 |  Summary | _____ | Implementing the notification functions is straightforward. As seen in the code snippets below, you have to get the alerter instance from your current |
| 488 |  References | _____ | Finally, we saw how to implement a monitoring pipeline using Opik from Comet ML and an alerting system using ZenML. These are the fundamental pillars  |
| 489 |  References | _____ | • Preston-Werner, T. (n.d.). Semantic Versioning 2.0.0. Semantic Versioning. https://semver. org/ • Ribeiro, M. T., Wu, T., Guestrin, C., & Singh, S.  |

## Done when

- Every content page in 430–489 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Operations accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER11` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
