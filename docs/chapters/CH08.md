# Chapter 8 — brief for Claude Code

> Chapter 8: Inference Optimization

- **Stage:** Inference (accent `#EF5C46`) — use `stageForChapter(8)` / `accentForChapter(8)`.
- **PDF pages:** 318–345  (**28 pages**, 17 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter8.tsx` exporting `const CHAPTER8: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 8.1 | 319 | `data/figures/p319.png` | Figure 8.1 – Inference process with decoder-only models. We provide “I have a dream” as |
| 8.2 | 321 | `data/figures/p321.png` | You can see an illustration of this technique in Figure 8.2: |
| 8.3 | 324 | `data/figures/p324.png` | Figure 8.3 – Illustration of traditional decoding (left) and speculative decoding (right) |
| 8.4 | 328 | `data/figures/p328.png` | Figure 8.4 – Illustration of data parallelism with four GPUs |
| 8.5 | 329 | `data/figures/p329.png` | Figure 8.5 – Illustration of pipeline parallelism with four GPUs |
| 8.6 | 330 | `data/figures/p330.png` | Figure 8.6 – Illustration of pipeline parallelism with micro-batching. |
| 8.7 | 331 | `data/figures/p331.png` | Figure 8.7 – Illustration of column-wise tensor parallelism in an MLP layer (W) |
| 8.8 | 332 | `data/figures/p332.png` | Figure 8.8 – Illustration of the different model parallelism techniques |
| 8.9 | 334 | `data/figures/p334.png` | Figure 8.9 – Comparison the between FP32, FP16, and BF16 formats |
| 8.10 | 335 | `data/figures/p335.png` | zation, can be applied to convert FP32, FP16, or BF16 weights to INT8, as illustrated in Figure 8.10: |
| 8.11 | 337 | `data/figures/p337.png` | an example of outliers in Figure 8.11: |
| 8.12 | 170 | `data/figures/p170.png` | Figure 8.12 shows the DAG of the RAG feature pipeline, where you can follow all the pipeline steps |
| 8.16 | 180 | `data/figures/p180.png` | As we can see in Figure 8.16, we will repeat the same logic for the Chunk and EmbeddedChunk base |
| 8.17 | 191 | `data/figures/p191.png` | handler, as shown in Figure 8.17. In total, we will have nine Handler classes that follow the next |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Python`, `Hugging Face`, `GitHub`, `Mistral`, `Llama`, `vLLM`, `Beam`, `TGI`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.319 — Model optimization strategies
  - p.320 — KV cache
  - p.323 — Continuous batching
  - p.324 — Speculative decoding
  - p.326 — Optimized attention mechanisms
- p.327 — Model parallelism
  - p.328 — Data parallelism
  - p.329 — Pipeline parallelism
  - p.330 — Tensor parallelism
  - p.332 — Combining approaches
- p.332 — Model quantization
  - p.333 — Introduction to quantization
  - p.338 — Quantization with GGUF and llama.cpp
  - p.340 — Quantization with GPTQ and EXL2
  - p.342 — Other quantization techniques
- p.343 — Summary
- p.344 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 318 |  Chapter 8: Inference Optimization | _____ | Inference Optimization Deploying LLMs is challenging due to their significant computational and memory requirements. Efficiently running these models  |
| 319 |  Model optimization strategies | _____ | By the end of this chapter, you will understand the core challenges in LLM inference and be fa- miliar with state-of-the-art optimization techniques,  |
| 320 |  KV cache | _____ | As shown in Figure 8.1, the basic inference process for a decoder-only model involves: 1. Tokenizing the input prompt and passing it through an embedd |
| 321 |  KV cache | _____ | You can see an illustration of this technique in Figure 8.2: Figure 8.2 – Illustration of the KV cache When a new token is generated, only the key and |
| 322 |  KV cache | _____ | Since the KV cache grows with each generation step and is dynamic, it prevents you from taking advantage of torch.compile, a powerful optimization too |
| 323 |  Continuous batching | _____ | This returns a list containing both the input and output, correctly answering our question. Efficiently managing the KV cache is essential, as it can  |
| 324 |  Speculative decoding | _____ | Speculative decoding Another powerful optimization technique is speculative decoding, also called assisted generation. The key insight is that even wi |
| 325 |  Speculative decoding | _____ | Here, we’re limited by the VRAM of the T4 GPU in Google Colab, but to get the maximum speedup, the assistant model should be much smaller than the lar |
| 326 |  Optimized attention mechanisms | _____ | Another approach to creating a small proxy model consists of jointly fine-tuning a small model alongside a large model for maximum fidelity. A represe |
| 327 |  Model parallelism | _____ | This is combined with online softmax, which computes the softmax function independently for each block of the attention scores matrix, rather than for |
| 328 |  Data parallelism | _____ | Although these approaches were originally developed for training, we can reuse them for inference by focusing on the forward pass only. Data paralleli |
| 329 |  Pipeline parallelism | _____ | Pipeline parallelism Introduced by Huang et al. in the GPipe paper (2019), pipeline parallelism (PP) is a strategy for distributing the computational  |
| 330 |  Tensor parallelism | _____ | Micro-batching was developed to mitigate the impact of pipeline bubbles. By splitting the input batch into smaller sub-batches, micro-batching ensures |
| 331 |  Tensor parallelism | _____ | In TP, large matrices, such as the weight matrices in MLPs or the attention heads in self-atten- tion layers, are partitioned across several GPUs. Eac |
| 332 |  Model quantization | _____ | Moreover, TP necessitates high-speed interconnects between devices to minimize communica- tion overhead, making it impractical to implement across nod |
| 333 |  Introduction to quantization | _____ | By default, weights are typically stored in a 16-bit or 32-bit floating-point format (FP16 or FP32), which provides high precision but comes at the co |
| 334 |  Introduction to quantization | _____ | Figure 8.9 – Comparison the between FP32, FP16, and BF16 formats A sign of 0 represents a positive number, while 1 indicates a negative number. Conver |
| 335 |  Introduction to quantization | _____ | However, we are not restricted to these three data types. Lower-precision data types, such as INT8 (8-bit integers), can be employed for quantization, |
| 336 |  Introduction to quantization | _____ | Zero-point quantization, on the other hand, considers asymmetric input distributions and maps the weights 0.0008 to the range [-128, 127] by introduci |
| 337 |  Introduction to quantization | _____ | Discarding these outliers is not feasible, as it would degrade a model’s performance. You can see an example of outliers in Figure 8.11: Figure 8.11 – |
| 338 |  Quantization with GGUF and llama.cpp | _____ | Introduced by Dettmers et al. (2023), NF4 is a 4-bit precision format designed for QLoRA (discussed in Chapter 5). It is also integrated into the tran |
| 339 |  Quantization with GGUF and llama.cpp | _____ | To provide a brief overview of GGUF quantization, llama.cpp groups values into blocks and rounds them to a lower precision. For instance, the legacy Q |
| 340 |  Quantization with GPTQ and EXL2 | _____ | 5. Once it’s done, your quantized model is ready. You can download it locally, or upload it to the Hugging Face Hub using the following code: from hug |
| 341 |  Quantization with GPTQ and EXL2 | _____ | GPTQ and EXL2 quants are based on the GPTQ algorithm, introduced by Frantar et al. (2023). It optimizes weight quantization for LLMs by refining the O |
| 342 |  Other quantization techniques | _____ | 4. Quantize the model at a given precision (for example, 4.5): !mkdir quant !python exllamav2/convert.py \ -i {MODEL_NAME} \ -o quant \ -c wikitext-te |
| 343 |  Summary | _____ | This trend is expected to continue, further optimizing these quantization methods. To conclude this chapter, here is a table summarizing the features  |
| 344 |  References | _____ | Way back in Chapter 4, we focused only on implementing the ingestion pipeline, which is just one component of a standard RAG application. In the next  |
| 345 |  References | _____ | • T. Dettmers, M. Lewis, Y. Belkada, L. Zettlemoyer, LLM.int8(): 8-bit Matrix Multiplication for Transformers at Scale, 2022. • G. Gerganov, llama.cpp |

## Done when

- Every content page in 318–345 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Inference accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER8` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
