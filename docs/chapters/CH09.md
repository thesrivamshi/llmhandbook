# Chapter 9 — brief for Claude Code

> Chapter 9: RAG Inference Pipeline

- **Stage:** Inference (accent `#EF5C46`) — use `stageForChapter(9)` / `accentForChapter(9)`.
- **PDF pages:** 346–383  (**38 pages**, 12 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter9.tsx` exporting `const CHAPTER9: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 9.1 | 348 | `data/figures/p348.png` | Figure 9.1: RAG inference pipeline architecture |
| 9.2 | 368 | `data/figures/p368.png` | Figure 9.2: Search logic of the RAG retrieval module |
| 9.3 | 369 | `data/figures/p369.png` | Figure 9.3: Processing the results flow of the RAG retrieval module |
| 9.4 | 377 | `data/figures/p377.png` | Figure 9.4: Routing and memory examples |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Python`, `Poetry`, `Qdrant`, `AWS`, `SageMaker`, `GitHub`, `FastAPI`, `Pydantic`, `LangChain`, `OpenAI`, `Llama`, `Superlinked`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.347 — Understanding the LLM twin’s RAG inference pipeline
- p.350 — Exploring the LLM twin’s advanced RAG techniques
  - p.353 — Advanced RAG pre-retrieval optimizations: query expansion and self-querying
    - p.353 — Query expansion
    - p.357 — Self-querying
  - p.361 — Advanced RAG retrieval optimization: filtered vector search
  - p.363 — Advanced RAG post-retrieval optimization: reranking
- p.367 — Implementing the LLM twin’s RAG inference pipeline
  - p.368 — Implementing the retrieval module
  - p.375 — Bringing everything together into the RAG inference pipeline
- p.380 — Summary
- p.380 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 346 |  Chapter 9: RAG Inference Pipeline | _____ | RAG Inference Pipeline Back in Chapter 4, we implemented the retrieval-augmented generation (RAG) feature pipeline to populate the vector database (DB |
| 347 |  Understanding the LLM twin’s RAG inferen | _____ | We will dedicate the next chapter entirely to deploying our fine-tuned LLM Twin model to AWS SageMaker, as an AWS SageMaker inference endpoint. Thus,  |
| 348 |  Understanding the LLM twin’s RAG inferen | _____ | Figure 9.1: RAG inference pipeline architecture The feature pipeline and the retrieval module, defined in Figure 9.1, are independent processes. The f |
| 349 |  Understanding the LLM twin’s RAG inferen | _____ | By separating concerns between the two components, the vector DB is always populated with the latest data, ensuring feature freshness, while the retri |
| 350 |  Exploring the LLM twin’s advanced RAG te | _____ | 7. Build the prompt and call the LLM: We map the final list of the most relevant K chunks to a string used to build the final prompt. We create the pr |
| 351 |  Exploring the LLM twin’s advanced RAG te | _____ | @abstractmethod def create_template(self) -> PromptTemplate: pass We also want to define a RAGStep interface used to standardize the interface of adva |
| 352 |  Exploring the LLM twin’s advanced RAG te | _____ | class Config: category = DataCategory.QUERIES What is essential to notice are the class’s attributes used to combine the user’s query with a bunch of  |
| 353 |  Query expansion | _____ | embedding: list[float] class Config: category = DataCategory.QUERIES The EmbeddedQuery class extends Query by adding the embedding field. The Embedded |
| 354 |  Query expansion | _____ | Implementing query expansion can be as straightforward as crafting a detailed zero-shot prompt to guide the LLM in generating these alternative querie |
| 355 |  Query expansion | _____ | In the generate method, we first ensure that the number of expansions requested (expand_to_n) is greater than zero. If the instance is in mock mode (s |
| 356 |  Query expansion | _____ | Finally, we define the QueryExpansionTemplate class, which constructs the prompt used for query expansion. The class and other prompt templates can be |
| 357 |  Self-querying | _____ | It takes expand_to_n as an input parameter to define how many queries we wish to generate while we build the PromptTemplate instance. The separator pr |
| 358 |  Self-querying | _____ | This problem stands for any other metadata you want to present during the search, such as IDs, names, or categories. The solution is to use self-query |
| 359 |  Self-querying | _____ | Here, we instantiate the prompt using the SelfQueryTemplate factory class and create a ChatOpenAI model instance (similar to the query expansion imple |
| 360 |  Self-querying | _____ | class SelfQueryTemplate(PromptTemplateFactory): prompt: str = """You are an AI language model assistant. Your task is to extract information from a us |
| 361 |  Advanced RAG retrieval optimization: fil | _____ | By implementing self-querying, we ensure that critical metadata required for our use case is ex- plicitly extracted and used during retrieval. This ap |
| 362 |  Advanced RAG retrieval optimization: fil | _____ | This exhaustive search slows response times and consumes more computational resources, making it inefficient for real-time or large-scale applications |
| 363 |  Advanced RAG post-retrieval optimization | _____ | ) ] ), ) In essence, while plain vector search provides a foundation for semantic retrieval, its limitations can slow performance in practical applica |
| 364 |  Advanced RAG post-retrieval optimization | _____ | Ultimately, we pick the top K most relevant chunks from the sorted list of N x K items based on the reranking score. Reranking works well when combine |
| 365 |  Advanced RAG post-retrieval optimization | _____ | In the initializer of the Reranker class, we instantiate our cross-encoder model by creating an instance of CrossEncoderModelSingleton. This is the cr |
| 366 |  Advanced RAG post-retrieval optimization | _____ | We import the CrossEncoder class from the sentence_transformers library, which provides the functionality for scoring text pairs. We also import Singl |
| 367 |  Implementing the LLM twin’s RAG inferenc | _____ | The __call__ method allows us to pass in a list of text pairs (each consisting of the query and a document chunk) and receive their relevance scores.  |
| 368 |  Implementing the retrieval module | _____ | Implementing the retrieval module Let’s dive into the ContextRetriever class implementation, which orchestrates the retrieval step in our RAG system b |
| 369 |  Implementing the retrieval module | _____ | The entry point function of the ContextRetriever class is the search() method, which calls all the advanced steps discussed in this chapter. Figure 9. |
| 370 |  Implementing the retrieval module | _____ | Figure 9.3 illustrates how we process the results returned by the xN searches. As each search returns ≤ K items, we will end up with ≤ N x K chunks th |
| 371 |  Implementing the retrieval module | _____ | num_queries=len(n_generated_queries), ) We then perform the search concurrently for all expanded queries using a thread pool. Each que- ry is processe |
| 372 |  Implementing the retrieval module | _____ | if embedded_query.author_id: query_filter = Filter( must=[ FieldCondition( key="author_id", match=MatchValue( value=str(embedded_query.author_id), ),  |
| 373 |  Implementing the retrieval module | _____ | retrieved_chunks = post_chunks + articles_chunks + repositories_ chunks return retrieved_chunks Finally, the rerank() method takes the original query  |
| 374 |  Implementing the retrieval module | _____ | retriever = ContextRetriever(mock=False) documents = retriever.search(query, k=3) logger.info("Retrieved documents:") for rank, document in enumerate( |
| 375 |  Bringing everything together into the RA | _____ | com/decodingml/the-4-advanced-rag-algorithms-you-must-know-to-implement- 5d0c7f1199d2' As you can observe in the output above, along with the retrieve |
| 376 |  Bringing everything together into the RA | _____ | prompt = f""" You are a content creator. Write what the user asked you to while using the provided context as the primary source of information for th |
| 377 |  Bringing everything together into the RA | _____ | Therefore, another way to add the chat history to your prompt is to keep a summary of the conver- sation along with the latest K replies. There are mu |
| 378 |  Bringing everything together into the RA | _____ | Another example would be if we want to illustrate a piece of code that shows how to build a RAG pipeline. In this case, the router would have to predi |
| 379 |  Bringing everything together into the RA | _____ | For example, in our LLM Twin use case, we used only the content field of our articles, posts, or repositories to query the vector DB. When using a mul |
| 380 |  References | _____ | Superlinked is a powerful Python tool for any use case that includes vector computing, such as RAG, recommender systems, and semantic search. It offer |
| 381 |  References | _____ | • How to do “self-querying” retrieval / LangChain. (n.d.). https://python.langchain.com/ docs/how_to/self_query/ • How to route between sub-chains / L |
| 382 |  References | _____ | Join our book’s Discord space Join our community’s Discord space for discussions with the authors and other readers: https://packt.link/llmeng |
| 383 |  References | _____ |  |

## Done when

- Every content page in 346–383 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Inference accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER9` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
