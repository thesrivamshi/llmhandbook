# Chapter 3 — brief for Claude Code

> Chapter 3: Data Engineering

- **Stage:** Feature / Data (accent `#15A864`) — use `stageForChapter(3)` / `accentForChapter(3)`.
- **PDF pages:** 84–127  (**44 pages**, 19 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter3.tsx` exporting `const CHAPTER3: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 3.1 | 85 | `data/figures/p85.png` | architecture, illustrated in Figure 3.1. We must explore what platforms we will crawl to extract |
| 3.2 | 87 | `data/figures/p87.png` | Figure 3.2. First, we will explore the three fundamental data categories we will work with across |
| 3.3 | 91 | `data/figures/p91.png` | Figure 3.3: Example of a digital_data_etl pipeline run from ZenML’s dashboard |
| 3.4 | 95 | `data/figures/p95.png` | Figure 3.4: The relationship between the provided links, the CrawlerDispatcher, and the |
| 3.5 | 120 | `data/figures/p120.png` | Figure 3.5: Example of the user output artifact after running the data collection pipeline using |
| 3.6 | 121 | `data/figures/p121.png` | Figure 3.6: Example of the crawled_links output artifact after running the data collection |
| 3.7 | 124 | `data/figures/p124.png` | URLs, as illustrated in Figure 3.7. You can leave out the Substack or personal blog URLs as these |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Python`, `Poetry`, `ZenML`, `MongoDB`, `Qdrant`, `AWS`, `Docker`, `GitHub`, `Selenium`, `FastAPI`, `Pydantic`, `LangChain`, `Pulumi`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.85 — Designing the LLM Twin’s data collection pipeline
  - p.90 — Implementing the LLM Twin’s data collection pipeline
  - p.90 — ZenML pipeline and steps
  - p.95 — The dispatcher: How do you instantiate the right crawler?
  - p.98 — The crawlers
    - p.98 — Base classes
    - p.102 — GitHubCrawler class
    - p.104 — CustomArticleCrawler class
    - p.106 — MediumCrawler class
  - p.108 — The NoSQL data warehouse documents
    - p.109 — The ORM and ODM software patterns
    - p.111 — Implementing the ODM class
    - p.116 — Data categories and user document classes
- p.118 — Gathering raw data into the data warehouse
  - p.123 — Troubleshooting
    - p.124 — Selenium issues
    - p.124 — Import our backed-up data
- p.125 — Summary
- p.125 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 84 |  Chapter 3: Data Engineering | _____ | Data Engineering This chapter will begin exploring the LLM Twin project in more depth. We will learn how to design and implement the data collection p |
| 85 |  Designing the LLM Twin’s data collection | _____ | Finally, we will explore how to run the data collection pipeline using ZenML and query the col- lected data from MongoDB. Thus, in this chapter, we wi |
| 86 |  Designing the LLM Twin’s data collection | _____ | Figure 3.1: LLM Twin’s data collection ETL pipeline architecture We want to design an ETL pipeline that inputs a user and a list of links as input. Af |
| 87 |  Designing the LLM Twin’s data collection | _____ | Hence, the signature of the data collection pipeline will look as follows: • Input: A list of links and their associated user (the author) • Output: A |
| 88 |  Designing the LLM Twin’s data collection | _____ | Our codebase supports four different crawlers: • Medium crawler: Used to collect data from Medium. It outputs an article document. It logs in to Mediu |
| 89 |  Designing the LLM Twin’s data collection | _____ | For our proof of concept, crawling a few hundred documents is enough, but if we want to scale it to a real-world product, we would probably need more  |
| 90 |  ZenML pipeline and steps | _____ | Implementing the LLM Twin’s data collection pipeline As we presented in Chapter 2, the entry point to each pipeline from our LLM Twin project is a Zen |
| 91 |  ZenML pipeline and steps | _____ | Figure 3.3 shows a run of the digital_data_etl pipeline on the ZenML dashboard. The next phase is to explore the get_or_create_user and crawl_links Ze |
| 92 |  ZenML pipeline and steps | _____ | Using a utility function, we split the full name into first and last names. Then, we attempt to retrieve the user from the database or create a new on |
| 93 |  ZenML pipeline and steps | _____ | from tqdm import tqdm from typing_extensions import Annotated from zenml import get_step_context, step from llm_engineering.application.crawlers.dispa |
| 94 |  ZenML pipeline and steps | _____ | links.") return links The code includes a helper function that attempts to extract information from each link using the appropriate crawler based on t |
| 95 |  The dispatcher: How do you instantiate t | _____ | The dispatcher: How do you instantiate the right crawler? The entry point to our crawling logic is the CrawlerDispatcher class. As illustrated in Figu |
| 96 |  The dispatcher: How do you instantiate t | _____ | We begin by importing the necessary Python modules for URL handling and regex, along with importing our crawler classes: import re from urllib.parse i |
| 97 |  The dispatcher: How do you instantiate t | _____ | return self def register_linkedin(self) -> "CrawlerDispatcher": self.register("https://linkedin.com", LinkedInCrawler) return self def register_github |
| 98 |  Base classes | _____ | The crawlers Before exploring each crawler’s implementation, we must present their base class, which defines a unified interface for all the crawlers. |
| 99 |  Base classes | _____ | Selenium can programmatically control various browsers such as Chrome, Firefox, or Brave. For these specific platforms, we need Selenium to manipulate |
| 100 |  Base classes | _____ | Its constructor initializes various Chrome options to optimize performance, enhance security, and ensure a headless browsing environment. These option |
| 101 |  Base classes | _____ | pass def login(self) -> None: pass Finally, the scroll_page() method implements a scrolling mechanism to navigate through pages, such as LinkedIn, up  |
| 102 |  GitHubCrawler class | _____ | GitHubCrawler class The GithubCrawler class is designed to scrape GitHub repositories, extending the functionality of the BaseCrawler. We don’t have t |
| 103 |  GitHubCrawler class | _____ | os.chdir(local_temp) subprocess.run(["git", "clone", link]) After successfully cloning the repository, the crawler constructs the path to the cloned r |
| 104 |  CustomArticleCrawler class | _____ | Finally, whether the scraping succeeds or an exception occurs, the crawler ensures that the tem- porary directory is removed to clean up any resources |
| 105 |  CustomArticleCrawler class | _____ | logger.info(f"Article already exists in the database: {link}") return If the article doesn’t exist, we proceed to scrape it. We use the AsyncHtmlLoade |
| 106 |  MediumCrawler class | _____ | instance = self.model( content=content, link=link, platform=platform, author_id=user.id, author_full_name=user.full_name, ) instance.save() logger.inf |
| 107 |  MediumCrawler class | _____ | If the article is new, the method proceeds to navigate to the article’s link and scroll through the page to ensure all content is loaded: def extract( |
| 108 |  The NoSQL data warehouse documents | _____ | platform="medium", content=data, link=link, author_id=user.id, author_full_name=user.full_name, ) instance.save() logger.info(f"Successfully scraped a |
| 109 |  The ORM and ODM software patterns | _____ | By leveraging Python packages such as Pydantic, we have out-of-the-box type validation, which ensures consistency in our datasets. Thus, we modeled th |
| 110 |  The ORM and ODM software patterns | _____ | # Define a class that maps to the users table. class User(Base): __tablename__ = "users" id = Column(Integer, primary_key=True) name = Column(String)  |
| 111 |  Implementing the ODM class | _____ | To conclude, ODM simplifies working with document-based NoSQL databases and maps ob- ject-oriented code to JSON-like documents. We will implement a li |
| 112 |  Implementing the ODM class | _____ | The NoSQLBaseDocument class is then declared as an abstract base class inheriting from Pydan- tic’s BaseModel, Python’s Generic (which provides the fu |
| 113 |  Implementing the ODM class | _____ | exclude_unset = kwargs.pop("exclude_unset", False) by_alias = kwargs.pop("by_alias", True) parsed = self.model_dump(exclude_unset=exclude_unset, by_al |
| 114 |  Implementing the ODM class | _____ | instance = collection.find_one(filter_options) if instance: return cls.from_mongo(instance) new_instance = cls(**filter_options) new_instance = new_in |
| 115 |  Implementing the ODM class | _____ | if instance: return cls.from_mongo(instance) return None except errors.OperationFailure: logger.error("Failed to retrieve document.") return None Simi |
| 116 |  Data categories and user document classe | _____ | We can configure each subclass using the nested Settings class, such as defining the collection name, or anything else specific to that subclass. With |
| 117 |  Data categories and user document classe | _____ | INSTRUCT_DATASET = "instruct_dataset" PREFERENCE_DATASET_SAMPLES = "preference_dataset_samples" PREFERENCE_DATASET = "preference_dataset" POSTS = "pos |
| 118 |  Gathering raw data into the data warehou | _____ | class ArticleDocument(Document): link: str class Settings: name = DataCategory.ARTICLES Finally, we define the UserDocument class, which is used to st |
| 119 |  Gathering raw data into the data warehou | _____ | We configured a different pipeline run for each author. We provided a ZenML configuration file for Paul Iusztin’s or Maxime Labonne’s data. To call th |
| 120 |  Gathering raw data into the data warehou | _____ | Figure 3.5: Example of the user output artifact after running the data collection pipeline using Maxime’s configuration file Also, in Figure 3.6, you  |
| 121 |  Gathering raw data into the data warehou | _____ | We want to highlight again the power of these artifacts, as they trace each pipeline’s results and metadata, making it extremely easy to monitor and d |
| 122 |  Gathering raw data into the data warehou | _____ | For example, we can easily run the same data collection pipeline but with Paul Iusztin’s YAML configuration, listed below: parameters: user_full_name: |
| 123 |  Troubleshooting | _____ | user = UserDocument.get_or_create(first_name="Paul", last_name="Iusztin") articles = ArticleDocument.bulk_find(author_id=str(user.id)) print(f"User ID |
| 124 |  Import our backed-up data | _____ | Selenium issues It is a well-known issue that running Selenium can cause problems due to issues with the browser driver, such as the ChromeDriver. Thu |
| 125 |  References | _____ | After running the CLI command from above, you will have a one-to-one replica of the dataset we used while developing the code. To ensure the import is |
| 126 |  References | _____ | • Ramos, L. P. (2023, August 4). Python and MongoDB: Connecting to NoSQL Databases. https://realpython.com/introduction-to-mongodb-and-python/ • Refac |
| 127 |  References | _____ |  |

## Done when

- Every content page in 84–127 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Feature / Data accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER3` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
