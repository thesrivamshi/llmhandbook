# Chapter 6 — brief for Claude Code

> Chapter 6: Fine-Tuning with Preference Alignment

- **Stage:** Training (accent `#EE9613`) — use `stageForChapter(6)` / `accentForChapter(6)`.
- **PDF pages:** 258–289  (**32 pages**, 14 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter6.tsx` exporting `const CHAPTER6: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 6.1 | 259 | `data/figures/p259.png` | of our samples. To achieve this, we follow the same stages, as outlined in Figure 6.1: data curation, |
| 6.2 | 267 | `data/figures/p267.png` | Figure 6.2 – Synthetic data generation pipeline from raw text to preference dataset |
| 6.3 | 274 | `data/figures/p274.png` | Figure 6.3 – Screenshot of the mlabonne/llmtwin-dpo preference dataset on the Hugging |
| 6.4 | 276 | `data/figures/p276.png` | Figure 6.4 – High-level view of the PPO algorithm for preference alignment |
| 6.5 | 278 | `data/figures/p278.png` | Figure 6.5 – High-level view of the DPO algorithm for preference alignment |
| 6.6 | 285 | `data/figures/p285.png` | Figure 6.6 – Experiment tracking in Comet ML with DPO metrics |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Poetry`, `Hugging Face`, `Comet ML`, `Comet`, `GitHub`, `OpenAI`, `Mistral`, `Llama`, `Unsloth`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.259 — Understanding preference datasets
  - p.259 — Preference data
    - p.261 — Data quantity
  - p.262 — Data generation and evaluation
    - p.262 — Generating preferences
    - p.263 — Tips for data generation
    - p.264 — Evaluating preferences
- p.266 — Creating our own preference dataset
- p.274 — Preference alignment
  - p.275 — Reinforcement Learning from Human Feedback
  - p.277 — Direct Preference Optimization
- p.279 — Implementing DPO
- p.286 — Summary
- p.287 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 258 |  Chapter 6: Fine-Tuning with Preference A | _____ | Fine-Tuning with Preference Alignment Supervised Fine-Tuning (SFT) has been crucial in adapting LLMs to perform specific tasks. How- ever, SFT struggl |
| 259 |  Preference data | _____ | By the end of this chapter, you will be able to create your own preference datasets and align models with diverse techniques. Understanding preference |
| 260 |  Preference data | _____ | Instruction Tell me a joke about octopuses. Chosen answer Why don’t octopuses play cards in casinos? Because they can’t count past eight. Rejected ans |
| 261 |  Data quantity | _____ | • Creative writing: For tasks like story generation or poetry writing, the quality of the output is highly subjective and multifaceted. Preference dat |
| 262 |  Generating preferences | _____ | On the other hand, tasks like the ones previously described require fewer preference pairs. Task-specific alignment focuses on improving model perform |
| 263 |  Tips for data generation | _____ | • LLM-generated, human-evaluated datasets: This method offers a good balance between quality and efficiency. LLMs generate multiple responses to promp |
| 264 |  Evaluating preferences | _____ | For instance, when generating summaries, one might request variations such as concise sum- maries, detailed summaries, and summaries focusing on key p |
| 265 |  Evaluating preferences | _____ | For absolute scoring, you would create a prompt that outlines the evaluation criteria and asks the LLM to rate the response on a specific scale (e.g., |
| 266 |  Creating our own preference dataset | _____ | However, it’s important to note that LLM-based evaluation can be subject to several types of bias: • Position bias: In relative scoring, LLM judges te |
| 267 |  Creating our own preference dataset | _____ | As seen in the previous section, preference and instruction datasets rely on the same principles. Instead of pairs of instructions and answers, we nee |
| 268 |  Creating our own preference dataset | _____ | 2. Instead of the InstructionAnswerSet class, we now have a PreferenceSet class. This class is designed to handle triples of instructions, generated a |
| 269 |  Creating our own preference dataset | _____ | "author_full_name": [item["author_full_name"] for item in data["artifact_data"]], "link": [item["link"] for item in data["artifact_ data"]], } ) 4. Th |
| 270 |  Creating our own preference dataset | _____ | current_chunk = sentence + " " if len(current_chunk) >= min_length: extracts.append(current_chunk.strip()) return extracts 6. The generate_preference_ |
| 271 |  Creating our own preference dataset | _____ | "generated_answer": "...", "extracted_answer": "..." }}, ... ] }} Extract: {extract} """ 7. In the same function, we use GPT-4o-mini to generate our a |
| 272 |  Creating our own preference dataset | _____ | 8. Two new filtering functions are introduced for the preference data pipeline: filter_short_ answers and filter_answer_format. These functions filter |
| 273 |  Creating our own preference dataset | _____ | preference_triples.extend(future.result()) instructions, generated_answers, extracted_answers = zip(*preference_triples) return Dataset.from_dict( { " |
| 274 |  Preference alignment | _____ | The create_preference_dataset() function generated 2,970 samples. This dataset is then heav- ily filtered to only retain 1,467 samples by removing ans |
| 275 |  Reinforcement Learning from Human Feedba | _____ | Reinforcement Learning from Human Feedback Reinforcement Learning from Human Feedback (RLHF) combines reinforcement learning (RL) with human input to  |
| 276 |  Reinforcement Learning from Human Feedba | _____ | The learned reward model serves as a proxy for human preferences, enabling the RL algorithm to train continuously without direct human input for every |
| 277 |  Direct Preference Optimization | _____ | Direct Preference Optimization Introduced by Rafailov et al. in their 2023 paper Direct Preference Optimization: Your Language Model is Secretly a Rew |
| 278 |  Direct Preference Optimization | _____ | Figure 6.5 – High-level view of the DPO algorithm for preference alignment DPO has several advantages over traditional RLHF methods. As previously men |
| 279 |  Implementing DPO | _____ | While RLHF allows iterative improvement through multiple training rounds and can dynamically adapt to new preferences, DPO offers a more straightforwa |
| 280 |  Implementing DPO | _____ | The dependencies are the same as those in Chapter 5 with SFT and can be found in the book’s GitHub repository (https://github.com/PacktPublishing/LLM- |
| 281 |  Implementing DPO | _____ | 6. Let’s now prepare the model for PEFT with the LoRA configuration. We increase the rank (r) and lora_alpha from 32 (as it was in Chapter 5) to 64. T |
| 282 |  Implementing DPO | _____ | example["rejected"] = example['rejected'] + EOS_TOKEN return {"prompt": example["prompt"], "chosen": example["chosen"], "rejected": example["rejected" |
| 283 |  Implementing DPO | _____ | fp16=not is_bfloat16_supported(), bf16=is_bfloat16_supported(), optim="adamw_8bit", weight_decay=0.01, warmup_steps=10, output_dir="output", eval_stra |
| 284 |  Implementing DPO | _____ | We can compare it with the answer provided by the SFT model: Supervised fine-tuning is a method used to enhance a language model by providing it with  |
| 285 |  Implementing DPO | _____ | Figure 6.6 – Experiment tracking in Comet ML with DPO metrics Let’s review these metrics: • Training loss: We still want the loss to continuously decr |
| 286 |  Summary | _____ | • Accuracies: This metric represents the percentage of times the model correctly identifies the chosen answers. We want this accuracy to gradually inc |
| 287 |  References | _____ | References • Rafael Rafailov et al.. “Direct Preference Optimization: Your Language Model is Secretly a Reward Model.” arXiv preprint arXiv:2305.18290 |
| 288 |  References | _____ | Join our book’s Discord space Join our community’s Discord space for discussions with the authors and other readers: https://packt.link/llmeng |
| 289 |  References | _____ |  |

## Done when

- Every content page in 258–289 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Training accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER6` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
