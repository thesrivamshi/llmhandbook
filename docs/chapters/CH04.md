# Chapter 4 — brief for Claude Code

> Chapter 4: RAG Feature Pipeline

- **Stage:** Feature / Data (accent `#15A864`) — use `stageForChapter(4)` / `accentForChapter(4)`.
- **PDF pages:** 128–205  (**78 pages**, 47 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter4.tsx` exporting `const CHAPTER4: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 4.1 | 132 | `data/figures/p132.png` | Figure 4.1: Vanilla RAG architecture |
| 4.2 | 136 | `data/figures/p136.png` | Figure 4.2: What are embeddings? |
| 4.3 | 137 | `data/figures/p137.png` | Figure 4.3: Visualize embeddings using UMAP (Source: UMAP’s documentation) |
| 4.4 | 140 | `data/figures/p140.png` | Figure 4.4: Creating embeddings from an image using a CNN (Image source) |
| 4.5 | 147 | `data/figures/p147.png` | Figure 4.5: The three stages of advanced RAG |
| 4.6 | 149 | `data/figures/p149.png` | Figure 4.6: Query routing |
| 4.7 | 154 | `data/figures/p154.png` | Figure 4.7: Bi-encoder (the standard embedding model) versus cross-encoder |
| 4.8 | 153 | `data/figures/p153.png` | information using a re-raking model, as illustrated in Figure 4.8. |
| 4.9 | 158 | `data/figures/p158.png` | Figure 4.9: The architecture of the LLM Twin’s RAG feature pipeline |
| 4.10 | 163 | `data/figures/p163.png` | Figure 4.10: Tools on the streaming versus batch and smaller versus bigger data spectrum |
| 4.11 | 170 | `data/figures/p170.png` | Figure 4.11: Feature pipeline runs in the ZenML dashboard |
| 4.12 | 170 | `data/figures/p170.png` | Figure 4.12: Feature pipeline DAG in the ZenML dashboard |
| 4.13 | 175 | `data/figures/p175.png` | Figure 4.13: Metadata of the “query the data warehouse” ZenML step |
| 4.14 | 177 | `data/figures/p177.png` | Figure 4.14: Metadata of the embedding and chunking ZenML step, detailing the uncategorized |
| 4.15 | 178 | `data/figures/p178.png` | Figure 4.15: Metadata of the embedding and chunking ZenML step, detailing the embedding |
| 4.16 | 181 | `data/figures/p181.png` | Figure 4.16: Domain entities class hierarchy and their interaction |
| 4.17 | 192 | `data/figures/p192.png` | Figure 4.17: Handler class hierarchy and their interaction |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Python`, `Poetry`, `ZenML`, `Hugging Face`, `MongoDB`, `Qdrant`, `GitHub`, `Pydantic`, `LangChain`, `OpenAI`, `Bytewax`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.129 — Understanding RAG
  - p.129 — Why use RAG?
    - p.130 — Hallucinations
    - p.130 — Old information
  - p.130 — The vanilla RAG framework
    - p.133 — Ingestion pipeline
    - p.134 — Retrieval pipeline
    - p.134 — Generation pipeline
  - p.136 — What are embeddings?
    - p.138 — Why embeddings are so powerful
    - p.140 — How are embeddings created?
    - p.143 — Applications of embeddings
  - p.144 — More on vector DBs
    - p.144 — How does a vector DB work?
    - p.145 — Algorithms for creating the vector index
    - p.145 — DB operations
- p.146 — An overview of advanced RAG
  - p.148 — Pre-retrieval
  - p.151 — Retrieval
  - p.153 — Post-retrieval
- p.156 — Exploring the LLM Twin’s RAG feature pipeline architecture
  - p.156 — The problem we are solving
  - p.157 — The feature store
  - p.157 — Where does the raw data come from?
  - p.158 — Designing the architecture of the RAG feature pipeline
    - p.159 — Batch pipelines
    - p.159 — Batch versus streaming pipelines
    - p.163 — Core steps
    - p.165 — Change data capture: syncing the data warehouse and feature store
    - p.167 — Why is the data stored in two snapshots?
    - p.167 — Orchestration
- p.168 — Implementing the LLM Twin’s RAG feature pipeline
  - p.168 — Settings
  - p.169 — ZenML pipeline and steps
    - p.172 — Querying the data warehouse
    - p.175 — Cleaning the documents
    - p.176 — Chunk and embed the cleaned documents
    - p.179 — Loading the documents to the vector DB
  - p.179 — Pydantic domain entities
    - p.183 — OVM
  - p.189 — The dispatcher layer
  - p.191 — The handlers
    - p.192 — The cleaning handlers
    - p.194 — The chunking handlers
    - p.198 — The embedding handlers
- p.202 — Summary
- p.203 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 128 |  Chapter 4: RAG Feature Pipeline | _____ | RAG Feature Pipeline Retrieval-augmented generation (RAG) is fundamental in most generative AI applications. RAG’s core responsibility is to inject cu |
| 129 |  Why use RAG? | _____ | Understanding RAG RAG enhances the accuracy and reliability of generative AI models with information fetched from external sources. It is a technique  |
| 130 |  The vanilla RAG framework | _____ | Hallucinations If a chatbot without RAG is asked a question about something it wasn’t trained on, there is a high chance that it will give you a confi |
| 131 |  The vanilla RAG framework | _____ | A RAG system is composed of three main modules independent of each other: • Ingestion pipeline: A batch or streaming pipeline used to populate the vec |
| 132 |  The vanilla RAG framework | _____ | Figure 4.1: Vanilla RAG architecture You must implement RAG in your generative AI application when you need access to any type of external information |
| 133 |  Ingestion pipeline | _____ | Ingestion pipeline The RAG ingestion pipeline extracts raw documents from various data sources (e.g., data ware- house, data lake, web pages, etc.). T |
| 134 |  Generation pipeline | _____ | Retrieval pipeline The retrieval components take the user’s input (text, image, audio, etc.), embed it, and query the vector DB for similar vectors to |
| 135 |  Generation pipeline | _____ | The final prompt results from a system and prompt template populated with the user’s query and retrieved context. You might have a single prompt templ |
| 136 |  What are embeddings? | _____ | What are embeddings? Imagine you’re trying to teach a computer to understand the world. Embeddings are like a par- ticular translator that turns these |
| 137 |  What are embeddings? | _____ | For example, you can use UMAP (https://umap-learn.readthedocs.io/en/latest/index. html), a dimensionality reduction method well known for keeping the  |
| 138 |  Why embeddings are so powerful | _____ | Why embeddings are so powerful Firstly, ML models work only with numerical values. This is not a problem when working with tabular data, as the data i |
| 139 |  Why embeddings are so powerful | _____ | Secondly, embedding your input reduces the size of its dimension and condenses all of its se- mantic meaning into a dense vector. This is an extremely |
| 140 |  How are embeddings created? | _____ | Finally, a fully connected layer at the end takes all this processed information and transforms it into the final vector embedding, a numerical image  |
| 141 |  How are embeddings created? | _____ | In the code snippet below, you can see how we loaded a model from SentenceTransformer, comput- ed the embeddings for three sentences, and, ultimately, |
| 142 |  How are embeddings created? | _____ | The best-performing embedding model can change with time and your specific use case. You can find particular models on the Massive Text Embedding Benc |
| 143 |  Applications of embeddings | _____ | image = Image.open(BytesIO(response.content)) model = SentenceTransformer("clip-ViT-B-32") img_emb = model.encode(image) text_emb = model.encode( ["A  |
| 144 |  How does a vector DB work? | _____ | • Classification by using the embeddings as features • Zero-shot classification by comparing the embedding of each class and picking the most similar  |
| 145 |  DB operations | _____ | 3. Post-processing results: After identifying potential matches, the results undergo post-pro- cessing to refine accuracy. This step ensures that the  |
| 146 |  An overview of advanced RAG | _____ | • Monitoring: Continuous monitoring of DB performance, including query latency and re- source usage (RAM, CPU, disk), helps maintain optimal operation |
| 147 |  An overview of advanced RAG | _____ | • Post-retrieval: This stage mainly targets different ways to filter out noise from the retrieved documents and compress the prompt before feeding it  |
| 148 |  Pre-retrieval | _____ | Pre-retrieval The pre-retrieval steps are performed in two different ways: • Data indexing: It is part of the RAG ingestion pipeline. It is mainly imp |
| 149 |  Pre-retrieval | _____ | The intuition behind this is that if we use the whole text for computing the embedding, we might introduce too much noise, or the text could contain m |
| 150 |  Pre-retrieval | _____ | As illustrated in Figure 4.6, let’s assume that, based on the user’s input, to do RAG, we can retrieve additional context from a vector DB using vecto |
| 151 |  Retrieval | _____ | Both data indexing and query optimization pre-retrieval optimization techniques depend highly on your data type, structure, and source. Thus, as with  |
| 152 |  Retrieval | _____ | embeddings = model.encode([[instruction, sentence]]) print(embeddings.shape) # noqa # Output: (1, 768) The source code can be found at https://github. |
| 153 |  Post-retrieval | _____ | In practice, on the retrieval side, you usually start with filtered vector search or hybrid search, as they are fairly quick to implement. This approa |
| 154 |  Post-retrieval | _____ | Figure 4.7: Bi-encoder (the standard embedding model) versus cross-encoder The abovementioned techniques are far from an exhaustive list of all potent |
| 155 |  Post-retrieval | _____ | For example, if you work with multi-modal data such as text and images, most of the techniques from earlier won’t work as they are designed for text o |
| 156 |  The problem we are solving | _____ | Exploring the LLM Twin’s RAG feature pipeline architecture Now that you have a strong intuition and understanding of RAG and its workings, we will con |
| 157 |  Where does the raw data come from? | _____ | To conclude, we must design a feature pipeline that constantly syncs the data warehouse and logical feature store while processing the data accordingl |
| 158 |  Designing the architecture of the RAG fe | _____ | Designing the architecture of the RAG feature pipeline The last step is to architect and go through the design of the RAG feature pipeline of the LLM  |
| 159 |  Batch versus streaming pipelines | _____ | Batch pipelines A batch pipeline in data systems refers to a data processing method where data is collected, pro- cessed, and stored in predefined int |
| 160 |  Batch versus streaming pipelines | _____ | The core elements of streaming applications are a distributed event streaming platform such as Apache Kafka or Redpanda to store events from multiple  |
| 161 |  Batch versus streaming pipelines | _____ | For example, streaming pipelines are extremely powerful in social media recommender systems like TikTok. When using social media, user behavior change |
| 162 |  Batch versus streaming pipelines | _____ | The data collection pipeline used in the LLM Twin use case is another example of an ETL pipeline that extracts data from the internet, structures it,  |
| 163 |  Core steps | _____ | In Figure 8.10, we compare what tools you can use based on your architecture (streaming versus batch) and the quantity of data you have to process (sm |
| 164 |  Core steps | _____ | 2. Cleaning: The data from the data warehouse is standardized and partially clean, but we have to ensure that the text contains only useful informatio |
| 165 |  Change data capture: syncing the data wa | _____ | Change data capture: syncing the data warehouse and feature store As highlighted a few times in this chapter, data is constantly changing, which can r |
| 166 |  Change data capture: syncing the data wa | _____ | In summary, the push method is ideal for applications demanding immediate data access, where- as the pull method is better suited for large-scale data |
| 167 |  Orchestration | _____ | As this is an LLM book and not a data engineering one, we wanted to keep things simple, but it’s important to know that these techniques exist, and yo |
| 168 |  Settings | _____ | We will go into all the details of orchestration, scheduling, and CT in Chapter 11. Implementing the LLM Twin’s RAG feature pipeline The last step is  |
| 169 |  ZenML pipeline and steps | _____ | # QdrantDB Vector DB USE_QDRANT_CLOUD: bool = False QDRANT_DATABASE_HOST: str = "localhost" QDRANT_DATABASE_PORT: int = 6333 QDRANT_CLOUD_URL: str = " |
| 170 |  ZenML pipeline and steps | _____ | Figure 4.11 shows how multiple feature engineering pipeline runs look in ZenML’s dashboard. Figure 4.11: Feature pipeline runs in the ZenML dashboard  |
| 171 |  ZenML pipeline and steps | _____ | The final puzzle piece is understanding how to configure the RAG feature pipeline dynamically. All its available settings are exposed as function para |
| 172 |  Querying the data warehouse | _____ | Querying the data warehouse The first thing to notice is that a step is a Python function decorated with @step, similar to how a ZenML pipeline works. |
| 173 |  Querying the data warehouse | _____ | step_context.add_output_metadata(output_name="raw_documents", metadata=_get_metadata(documents)) return documents The fetch function leverages a threa |
| 174 |  Querying the data warehouse | _____ | logger.exception(f"'{query_name}' request failed.") results[query_name] = [] return results The _get_metadata() function takes the list of queried doc |
| 175 |  Cleaning the documents | _____ | You can always extend it with anything that makes sense for your use case. Figure 4.13: Metadata of the “query the data warehouse” ZenML step Cleaning |
| 176 |  Chunk and embed the cleaned documents | _____ | cleaned_document = CleaningDispatcher.dispatch(document) cleaned_documents.append(cleaned_document) step_context = get_step_context() step_context.add |
| 177 |  Chunk and embed the cleaned documents | _____ | metadata["num_embedded_chunks"] = len(embedded_chunks) step_context = get_step_context() step_context.add_output_metadata(output_name="embedded_docume |
| 178 |  Chunk and embed the cleaned documents | _____ | In Figure 4.15, the rest of the ZenML metadata from the embedding and chunking step details the embedding model and its properties used to compute the |
| 179 |  Pydantic domain entities | _____ | As ML systems can break at any time while in production due to drifts or untreated use cases, le- veraging the metadata section to monitor the ingeste |
| 180 |  Pydantic domain entities | _____ | We used Pydantic to model all our domain entities. When we wrote the book, choosing Pydantic was a no-brainer, as it is the go-to Python package for w |
| 181 |  Pydantic domain entities | _____ | The same will happen for the posts and repositories. Figure 4.16: Domain entities class hierarchy and their interaction We chose this design because t |
| 182 |  Pydantic domain entities | _____ | class CleanedPostDocument(CleanedDocument): image: Optional[str] = None class Config: name = "cleaned_posts" category = DataCategory.POSTS use_vector_ |
| 183 |  OVM | _____ | class EmbeddedChunk(VectorBaseDocument, ABC): content: str embedding: list[float] / None platform: str document_id: UUID4 author_id: UUID4 author_full |
| 184 |  OVM | _____ | Let’s take a look at the definition of the VectorBaseDocument class: from pydantic import UUID4, BaseModel from typing import Generic from llm_enginee |
| 185 |  OVM | _____ | vector = vector.tolist() return PointStruct(id=_id, vector=vector, payload=payload) • The VectorBaseDocument class inherits from Pydantic’s BaseModel  |
| 186 |  OVM | _____ | ) cls.create_collection() try: cls._bulk_insert(documents) except exceptions.UnexpectedResponse: logger.error(f"Failed to insert documents in '{cls.ge |
| 187 |  OVM | _____ | Now, we must define a method that lets us read all the records from the vector DB (without using vector similarity search logic). The bulk_find() meth |
| 188 |  OVM | _____ | **kwargs, ) documents = [cls.from_record(record) for record in records] if next_offset is not None: next_offset = UUID(next_offset, version=4) return  |
| 189 |  The dispatcher layer | _____ | **kwargs, ) documents = [cls.from_record(record) for record in records] return documents Now that we understand what our domain entities look like and |
| 190 |  The dispatcher layer | _____ | def create_handler(data_category: DataCategory) -> CleaningDataHandler: if data_category == DataCategory.POSTS: return PostCleaningHandler() elif data |
| 191 |  The handlers | _____ | Intuitively, in our dispatcher layer, the combination of the factory and strategy patterns works as follows: 1. Initially, we knew we wanted to clean  |
| 192 |  The cleaning handlers | _____ | Figure 4.17: Handler class hierarchy and their interaction Let’s examine each handler family and see how it is implemented. The cleaning handlers The  |
| 193 |  The cleaning handlers | _____ | @abstractmethod def clean(self, data_model: DocumentT) -> CleanedDocumentT: pass Now, for every post, article and repository, we have to implement a d |
| 194 |  The chunking handlers | _____ | content=clean_text(" #### ".join(data_model.content. values())), … # Copy the rest of the parameters from the data_model object. ) The handlers input  |
| 195 |  The chunking handlers | _____ | "chunk_size": 500, "chunk_overlap": 50, } @abstractmethod def chunk(self, data_model: CleanedDocumentT) -> list[ChunkT]: pass Let’s understand how the |
| 196 |  The chunking handlers | _____ | chunk_id = hashlib.md5(chunk.encode()).hexdigest() model = ArticleChunk( id=UUID(chunk_id, version=4), content=chunk, platform=data_model.platform, li |
| 197 |  The chunking handlers | _____ | if len(current_chunk) >= min_length: extracts.append(current_chunk.strip()) current_chunk = sentence + " " if len(current_chunk) >= min_length: extrac |
| 198 |  The embedding handlers | _____ | tokens_per_chunk=embedding_model.max_input_length, model_name=embedding_model.model_id, ) chunks_by_tokens = [] for section in text_split_by_character |
| 199 |  The embedding handlers | _____ | All data transformations logic for the embedding step is done here """ def embed(self, data_model: ChunkT) -> EmbeddedChunkT: return self.embed_batch( |
| 200 |  The embedding handlers | _____ | document_id=data_model.document_id, author_id=data_model.author_id, author_full_name=data_model.author_full_name, metadata={ "embedding_model_id": emb |
| 201 |  The embedding handlers | _____ | self._model_id, device=self._device, cache_folder=str(cache_dir) if cache_dir else None, ) self._model.eval() @property def model_id(self) -> str: ret |
| 202 |  Summary | _____ | embeddings = embeddings.tolist() return embeddings The embedding model class implements the singleton pattern (https://refactoring.guru/ design-patter |
| 203 |  References | _____ | References • Kenton, J.D.M.W.C. and Toutanova, L.K., 2019, June. Bert: Pre-training of deep bidirec- tional transformers for language understanding. I |
| 204 |  References | _____ | • Monigatti, L. (2024b, February 19). Advanced Retrieval-Augmented Generation: From Theory to LlaMaIndex Implementation. Medium. https://towardsdatasc |
| 205 |  References | _____ |  |

## Done when

- Every content page in 128–205 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Feature / Data accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER4` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
