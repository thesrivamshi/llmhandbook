# Chapter 5 — brief for Claude Code

> Chapter 5: Supervised Fine-Tuning

- **Stage:** Training (accent `#EE9613`) — use `stageForChapter(5)` / `accentForChapter(5)`.
- **PDF pages:** 206–257  (**52 pages**, 31 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter5.tsx` exporting `const CHAPTER5: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 5.1 | 207 | `data/figures/p207.png` | Figure 5.1 – Overview of the post-training data pipeline covered in this chapter |
| 5.2 | 209 | `data/figures/p209.png` | Figure 5.2 – Screenshot of the most-liked datasets on the Hugging Face Hub |
| 5.3 | 217 | `data/figures/p217.png` | reinforcement learning or traditional fine-tuning. Figure 5.3 shows ArmoRM-Llama3-8B-v0.1’s |
| 5.4 | 219 | `data/figures/p219.png` | Figure 5.4 – Argilla’s interface for collaborative data quality evaluation and exploration |
| 5.5 | 220 | `data/figures/p220.png` | Figure 5.5 – Representation of the historical TikTok dataset made with Nomic Atlas |
| 5.6 | 226 | `data/figures/p226.png` | Figure 5.6 – Synthetic data generation pipeline from raw text to instruction dataset |
| 5.7 | 234 | `data/figures/p234.png` | Figure 5.7 – The mlabonne/llmtwin instruction dataset on the Hugging Face Hub |
| 5.8 | 236 | `data/figures/p236.png` | Figure 5.8 – Basic flowchart to determine when fine-tuning is an option on a technical level |
| 5.9 | 240 | `data/figures/p240.png` | Figure 5.9 – Architectural differences of the three main SFT techniques at the module level |
| 5.10 | 242 | `data/figures/p242.png` | Figure 5.10 – LoRA adds the two trainable matrices 𝐴𝐴 and 𝐵𝐵 and keeps the pre-trained weights |
| 5.11 | 254 | `data/figures/p254.png` | Figure 5.11 – Four monitored metrics during fine-tuning in Comet ML |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Python`, `Hugging Face`, `Comet ML`, `Comet`, `SageMaker`, `GitHub`, `Pydantic`, `OpenAI`, `Mistral`, `Llama`, `Unsloth`, `RunPod`, `TGI`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.207 — Creating an instruction dataset
  - p.207 — General framework
    - p.209 — Data quantity
  - p.211 — Data curation
  - p.211 — Rule-based filtering
  - p.213 — Data deduplication
  - p.214 — Data decontamination
  - p.215 — Data quality evaluation
  - p.218 — Data exploration
  - p.220 — Data generation
  - p.222 — Data augmentation
- p.225 — Creating our own instruction dataset
- p.235 — Exploring SFT and its techniques
  - p.235 — When to fine-tune
  - p.237 — Instruction dataset formats
  - p.237 — Chat templates
  - p.240 — Parameter-efficient fine-tuning techniques
    - p.240 — Full fine-tuning
    - p.242 — LoRA
    - p.244 — QLoRA
  - p.245 — Training parameters
    - p.245 — Learning rate and scheduler
    - p.245 — Batch size
    - p.246 — Maximum length and packing
    - p.247 — Number of epochs
    - p.247 — Optimizers
    - p.248 — Weight decay
    - p.248 — Gradient checkpointing
- p.248 — Fine-tuning in practice
- p.255 — Summary
- p.256 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 206 |  Chapter 5: Supervised Fine-Tuning | _____ | Supervised Fine-Tuning Supervised Fine-Tuning (SFT) is a crucial step in preparing LLMs for real-world applications. Fol- lowing the initial pre-train |
| 207 |  General framework | _____ | Creating an instruction dataset In most use cases, creating an instruction dataset is the most difficult part of the fine-tuning process. This is due  |
| 208 |  General framework | _____ | System You are a helpful assistant, who always provide explanation. Think like you are answering to a five year old. Instruction Concepts: building, s |
| 209 |  Data quantity | _____ | In the following sections, we will see techniques to filter and evaluate instruction samples ac- cording to these dimensions. Data quantity The Huggin |
| 210 |  Data quantity | _____ | General-purpose models cover more topics, which requires additional samples. Among com- panies, we observe a wide range of values. For instance, Yi mo |
| 211 |  Rule-based filtering | _____ | Data curation When it comes to procuring data for fine-tuning, the approaches differ between task-specific and domain-specific models. For task-specif |
| 212 |  Rule-based filtering | _____ | Length filtering is a straightforward yet effective rule-based filtering technique. This meth- od involves setting thresholds for the acceptable lengt |
| 213 |  Data deduplication | _____ | Data deduplication Dataset diversity is fundamental to training models that can generalize well to new, unseen data. When a dataset contains duplicate |
| 214 |  Data decontamination | _____ | For more context-aware representations, language models like BERT, sentence transformers, or cross-encoders can generate embeddings for entire sentenc |
| 215 |  Data quality evaluation | _____ | Data quality evaluation Data quality evaluation is a critical aspect of machine learning, particularly for LLMs. The process involves assessing variou |
| 216 |  Data quality evaluation | _____ | Instruction You are a data quality evaluator. Your goal is to assess an instruction and its corresponding answer, determining how effectively the answ |
| 217 |  Data quality evaluation | _____ | In general, to improve evaluation reliability, strategies such as using multiple LLMs as a jury reduce bias and improve consistency. Leveraging a jury |
| 218 |  Data exploration | _____ | The Allen Institute for AI’s RewardBench leaderboard, hosted on Hugging Face (allenai/re- ward-bench), is a good resource for comparing different rewa |
| 219 |  Data exploration | _____ | Figure 5.4 shows an example with Argilla, a collaborative platform for manual data quality eval- uation and exploration. Figure 5.4 – Argilla’s interf |
| 220 |  Data generation | _____ | This makes sure that each topic is correctly covered for each programming language. Figure 5.5 – Representation of the historical TikTok dataset made  |
| 221 |  Data generation | _____ | Additionally, it serves as a method to augment underrepresented areas in a dataset, like insufficient examples of JavaScript error-handling techniques |
| 222 |  Data augmentation | _____ | An important aspect of synthetic data generation is the ability to control various attributes of the generated data. This includes factors such as the |
| 223 |  Data augmentation | _____ | In-depth evolving focuses on enhancing the complexity of existing instructions. It includes several techniques: • Constraints: It involves introducing |
| 224 |  Data augmentation | _____ | You are an Instruction Rewriter that rewrites the given #Instruction# into a more complex version. Please follow the steps below to rewrite the given  |
| 225 |  Creating our own instruction dataset | _____ | It then leverages advanced language models like GPT-4 to provide detailed critiques and numerical scores for these responses across multiple dimension |
| 226 |  Creating our own instruction dataset | _____ | To simplify this process and ensure properly structured results, we can employ structured gen- eration techniques. Structured generation is an effecti |
| 227 |  Creating our own instruction dataset | _____ | 2. We import all the required libraries as follows. import concurrent.futures import json import random import re from concurrent.futures import Threa |
| 228 |  Creating our own instruction dataset | _____ | If we simply load our dataset as a pandas dataframe, it returns the following table. id content platform author_id author_ full_ name link 0 ab2f9e2e- |
| 229 |  Creating our own instruction dataset | _____ | Finally, we implement strip() to remove any leading or trailing whitespace. def clean_text(text): text = re.sub(r"[^\w\s.,!?']", " ", text) text = re. |
| 230 |  Creating our own instruction dataset | _____ | if len(current_chunk) >= min_length: extracts.append(current_chunk.strip()) current_chunk = sentence + " " if len(current_chunk) >= min_length: extrac |
| 231 |  Creating our own instruction dataset | _____ | In our example, we want to create instructions like “Write a paragraph about X topic” and corresponding answers that are factual and imitate the write |
| 232 |  Creating our own instruction dataset | _____ | 8. In addition to the user prompt, we can also specify a system prompt to guide the mod- el into generating the expected instructions. Here, we repeat |
| 233 |  Creating our own instruction dataset | _____ | We use a default max_workers value of 4 because higher values tend to exceed OpenAI’s rate limits, potentially causing API request failures or throttl |
| 234 |  Creating our own instruction dataset | _____ | instruction_dataset = create_instruction_dataset(raw_dataset, client) print("Instruction dataset:") print(instruction_dataset.to_pandas()) # 3. Train/ |
| 235 |  When to fine-tune | _____ | As seen in the previous section, we could refine this instruction dataset by increasing the diver- sity and complexity of our samples. More advanced p |
| 236 |  When to fine-tune | _____ | Figure 5.8 – Basic flowchart to determine when fine-tuning is an option on a technical level Beyond these technical considerations, SFT answers common |
| 237 |  Chat templates | _____ | Instruction dataset formats Instruction datasets are stored in a particular format to organize instructions and answers. Typi- cally, each sample in t |
| 238 |  Chat templates | _____ | In general, they also include special tokens to identify the beginning and the end of a message, or who is the author of the message. Since base model |
| 239 |  Chat templates | _____ | A common issue with chat templates is that every single whitespace and line break is extremely important. Adding or removing any character would resul |
| 240 |  Full fine-tuning | _____ | Parameter-efficient fine-tuning techniques While many techniques exist in the literature, SFT has converged on three main techniques: full fine-tuning |
| 241 |  Full fine-tuning | _____ | • Optimizer states: Optimizer states are additional values maintained by optimization algorithms like Adam or AdamW. These typically include running a |
| 242 |  LoRA | _____ | LoRA LoRA is a parameter-efficient technique for fine-tuning LLMs. Developed to address the compu- tational challenges associated with adapting massiv |
| 243 |  LoRA | _____ | Mathematically, this can be represented as: 𝑊𝑊𝑊 𝑊 𝑊𝑊 𝑊 𝐵𝐵𝐵𝐵 Here, 𝑊𝑊 is the original weight matrix, 𝐵𝐵 and 𝐴𝐴 are the LoRA matrices,  |
| 244 |  QLoRA | _____ | Using LoRA, it’s possible to fine-tune a 7B parameter model on a single GPU with as little as 14- 18 GB of VRAM, depending on the specific configurati |
| 245 |  Batch size | _____ | The decision between QLoRA and LoRA should be based on the specific requirements of the project, available hardware, and the need to balance memory us |
| 246 |  Maximum length and packing | _____ | However, they also require more memory, which can be a limiting factor on GPUs with less VRAM. For instance, a batch size of 16 might work well on a h |
| 247 |  Optimizers | _____ | Packing maximizes the utilization of each training batch. Instead of assigning one sample per batch, packing combines multiple smaller samples into a  |
| 248 |  Fine-tuning in practice | _____ | Weight decay Weight decay works by adding a penalty for large weights to the loss function, encouraging the model to learn simpler, more generalizable |
| 249 |  Fine-tuning in practice | _____ | • License: Some model licenses only allow non-commercial work, which is a problem if we want to fine-tune for a company. Custom licenses are common in |
| 250 |  Fine-tuning in practice | _____ | To install the Unsloth library and its dependencies, we recommend directly installing from the GitHub repository of the book (https://github.com/Packt |
| 251 |  Fine-tuning in practice | _____ | 5. Now that the model is loaded, we can define our LoRA configuration. Here, we use a rank of 32 that is large enough to imitate the writing style and |
| 252 |  Fine-tuning in practice | _____ | {} ### Response: {}""" EOS_TOKEN = tokenizer.eos_token dataset = dataset.map(format_samples, batched=True, remove_ columns=dataset.column_names) 8. On |
| 253 |  Fine-tuning in practice | _____ | bf16=is_bfloat16_supported(), logging_steps=1, optim="adamw_8bit", weight_decay=0.01, warmup_steps=10, output_dir="output", report_to="comet_ml", seed |
| 254 |  Fine-tuning in practice | _____ | responses with human expectations, thereby improving its accuracy and relevance. The goal is to ensure that the model can respond effectively to a wid |
| 255 |  Summary | _____ | In particular, three of these metrics are important to monitor: • Training loss: It measures how well the model is performing on the task it’s being t |
| 256 |  References | _____ | We applied this framework to real-world data from Chapter 3, using an LLM to convert raw text into instruction-answer pairs. We then explored SFT tech |
| 257 |  References | _____ | • Edward J. Hu et al.. “LoRA: Low-Rank Adaptation of Large Language Models.” arXiv preprint arXiv:2106.09685, June 2021. • Tim Dettmers et al.. “QLoRA |

## Done when

- Every content page in 206–257 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Training accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER5` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
