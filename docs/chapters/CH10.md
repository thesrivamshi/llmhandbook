# Chapter 10 — brief for Claude Code

> Chapter 10: Inference Pipeline Deployment

- **Stage:** Inference (accent `#EF5C46`) — use `stageForChapter(10)` / `accentForChapter(10)`.
- **PDF pages:** 384–429  (**46 pages**, 27 headings)
- **Target:** one bespoke diagram per *content* page. Skip pure dividers, the references list, and the "Join our Discord" page.
- **Output file:** `src/diagrams/chapter10.tsx` exporting `const CHAPTER10: PageDiagram[]`, then register it in `src/book.ts`.

## Figures to redraw faithfully (schematic, NOT bitmap)

Look at the rendered PNG in `data/figures/p{page}.png`, then rebuild the meaning with the shape alphabet + brand logos.

| Figure | PDF page | PNG | Caption |
|---|---|---|---|
| 10.1 | 389 | `data/figures/p389.png` | Figure 10.1: The three fundamental architectures of inference deployment types |
| 10.2 | 393 | `data/figures/p393.png` | Figure 10.2: Monolithic versus microservices architecture in model serving |
| 10.3 | 395 | `data/figures/p395.png` | Figure 10.3: Scaling microservices independently based on compute requirements |
| 10.4 | 398 | `data/figures/p398.png` | Figure 10.4: Microservice deployment architecture of the LLM Twin’s inference pipeline |
| 10.5 | 405 | `data/figures/p405.png` | Figure 10.5: AWS SageMaker deployment steps |
| 10.6 | 414 | `data/figures/p414.png` | in the Inference column, click on the Endpoints button, as illustrated in Figure 10.6. |
| 10.7 | 414 | `data/figures/p414.png` | status, as seen in Figure 10.7. After clicking on it, you can look at the endpoint’s logs in CloudWatch |
| 10.8 | 424 | `data/figures/p424.png` | Figure 10.8: Autoscaling possible use cases |

## Named tools introduced here (add to `TOOL_REGISTRY` with real logos)

`Python`, `Poetry`, `ZenML`, `Hugging Face`, `Comet`, `Qdrant`, `AWS`, `SageMaker`, `Bedrock`, `Docker`, `GitHub`, `FastAPI`, `Pydantic`, `OpenAI`, `Mistral`, `Llama`, `Terraform`, `TGI`

For each: try its `simple-icons` slug first; if missing, add a fallback pill with the vendor's brand colour (see DESIGN-SYSTEM.md). Specific products = real logos; generic ideas = shape alphabet.

## Sections (from the book's real outline)

- p.385 — Criteria for choosing deployment types
  - p.385 — Throughput and latency
  - p.386 — Data
- p.388 — Understanding inference deployment types
  - p.389 — Online real-time inference
  - p.390 — Asynchronous inference
  - p.391 — Offline batch transform
- p.392 — Monolithic versus microservices architecture in model serving
  - p.394 — Monolithic architecture
  - p.394 — Microservices architecture
  - p.396 — Choosing between monolithic and microservices architectures
- p.397 — Exploring the LLM Twin’s inference pipeline deployment strategy
  - p.400 — The training versus the inference pipeline
- p.401 — Deploying the LLM Twin service
  - p.402 — Implementing the LLM microservice using AWS SageMaker
    - p.402 — What are Hugging Face’s DLCs?
    - p.403 — Configuring SageMaker roles
    - p.404 — Deploying the LLM Twin model to AWS SageMaker
    - p.415 — Calling the AWS SageMaker Inference endpoint
  - p.419 — Building the business microservice using FastAPI
- p.422 — Autoscaling capabilities to handle spikes in usage
  - p.425 — Registering a scalable target
  - p.426 — Creating a scalable policy
  - p.427 — Minimum and maximum scaling limits
    - p.427 — Cooldown period
- p.428 — Summary
- p.429 — References

## Per-page worksheet (fill archetype + concept, then build the diagram)

Read the **full** cleaned text of each page in `data/pages.clean.json` before drawing — the excerpt below is only a hint. Pick ONE archetype per page from: single-concept · pipeline-flow · architecture · comparison · list-cluster · cycle · hierarchy · code-anatomy · formula-as-blocks · pitfall. No generic unlabeled boxes.

| Page | Section | Archetype (you fill) | First line of the real text (hint) |
|---|---|---|---|
| 384 |  Chapter 10: Inference Pipeline Deploymen | _____ | Inference Pipeline Deployment Deploying the inference pipeline for the large language model (LLM) Twin application is a critical stage in the machine  |
| 385 |  Throughput and latency | _____ | Hence, in this chapter, we will cover the following topics: • Criteria for choosing deployment types • Understanding inference deployment types • Mono |
| 386 |  Data | _____ | Low-latency systems require optimized and often more costly infrastructure, such as faster pro- cessors, lower network latency, and possibly edge comp |
| 387 |  Data | _____ | • Infrastructure must be optimized to reduce processing time to achieve low latency, such as using faster CPUs, GPUs, or specialized hardware. While o |
| 388 |  Understanding inference deployment types | _____ | Deeply thinking about these questions directly impacts the user experience of your application, which ultimately makes the difference between a succes |
| 389 |  Online real-time inference | _____ | With that in mind, let’s explore the three major ML deployment types. Figure 10.1: The three fundamental architectures of inference deployment types O |
| 390 |  Asynchronous inference | _____ | This approach is usually taken when serving models outside your internal network to the broader public. For example, OpenAI’s API implements a REST AP |
| 391 |  Offline batch transform | _____ | The client can either adopt a polling mechanism that checks on a schedule if there are new re- sults or adopt a push strategy and implement a notifica |
| 392 |  Monolithic versus microservices architec | _____ | Unlike the asynchronous inference architecture, a batch transform design is optimized for high throughput with permissive latency requirements. When r |
| 393 |  Monolithic versus microservices architec | _____ | But another aspect to consider is the architecture of the ML service itself, which can be imple- mented as a monolithic server or as multiple microser |
| 394 |  Microservices architecture | _____ | Monolithic architecture The LLM (or any other ML model) and the associated business logic (preprocessing and post-pro- cessing steps) are bundled into |
| 395 |  Microservices architecture | _____ | Thus, by decoupling the components, you can scale horizontally as required, with minimal costs, providing a cost-effective solution to your system’s n |
| 396 |  Choosing between monolithic and microser | _____ | The increased network communication between services can also introduce latency and potential points of failure, necessitating robust monitoring and r |
| 397 |  Exploring the LLM Twin’s inference pipel | _____ | Another option is to write the ML and business logic as two different Python packages that you glue together in the same ways as before. This is bette |
| 398 |  Exploring the LLM Twin’s inference pipel | _____ | The LLM microservice is strictly optimized for the RAG generation component. Ultimately, the business layer will send the prompt trace consisting of t |
| 399 |  Exploring the LLM Twin’s inference pipel | _____ | Let’s review the interface of the inference pipeline, which is defined by the feature/training/ inference (FTI) architecture. For the pipeline to run, |
| 400 |  The training versus the inference pipeli | _____ | The SageMaker Inference deployment is composed of the following components that we will show you how to implement: • SageMaker endpoint: An endpoint i |
| 401 |  Deploying the LLM Twin service | _____ | One key difference lies in how data is handled and accessed within each pipeline. During train- ing, data is typically accessed from offline storage i |
| 402 |  What are Hugging Face’s DLCs? | _____ | Thus, in this section, we will show you how to: • Deploy our fined-tuned LLM Twin model to AWS SageMaker • Write an inference client to interact with  |
| 403 |  Configuring SageMaker roles | _____ | • Continuous batching of incoming requests, thus improving throughput by dynamically batching requests as they arrive • Accelerated weight loading by  |
| 404 |  Deploying the LLM Twin model to AWS Sage | _____ | The last step is to take the new credentials from the JSON file and update the AWS_ACCESS_ KEY and AWS_SECRET_KEY variables in your .env file. You can |
| 405 |  Deploying the LLM Twin model to AWS Sage | _____ | In this section, we will walk you through the code presented in Figure 10.5 that helps us fully auto- mate the deployment process, starting with the c |
| 406 |  Deploying the LLM Twin model to AWS Sage | _____ | We will take a top-down approach to walk you through the implementation, starting with the main function that deploys the LLM Twin model to AWS SageMa |
| 407 |  Deploying the LLM Twin model to AWS Sage | _____ | Next, we implement the endpoint_config_exists method, checking whether a specific Sage- Maker endpoint configuration exists: def endpoint_config_exist |
| 408 |  Deploying the LLM Twin model to AWS Sage | _____ | ) self.resource_manager = resource_manager The deploy() method is the heart of the DeploymentService class. This method orchestrates the entire proces |
| 409 |  Deploying the LLM Twin model to AWS Sage | _____ | logger.info(f"Successfully deployed/updated model to endpoint {endpoint_name}.") except Exception as e: logger.error(f"Failed to deploy model to SageM |
| 410 |  Deploying the LLM Twin model to AWS Sage | _____ | endpoint_name=endpoint_name, update_endpoint=update_endpoint, resources=resources, tags=[{"Key": "task", "Value": "model_task"}], endpoint_type=endpoi |
| 411 |  Deploying the LLM Twin model to AWS Sage | _____ | resources: Optional[dict] = None, endpoint_type: enum.Enum = EndpointType.MODEL_BASED, ) -> None: logger.info("Starting deployment using Sagemaker Hug |
| 412 |  Deploying the LLM Twin model to AWS Sage | _____ | except Exception as e: logger.error(f"Error during deployment: {e}") raise Also, let’s review the resource configuration to understand the infrastruct |
| 413 |  Deploying the LLM Twin model to AWS Sage | _____ | By setting these parameters, the class ensures that it has sufficient resources to operate efficiently when the model is deployed to a SageMaker endpo |
| 414 |  Deploying the LLM Twin model to AWS Sage | _____ | That’s all you need to deploy an inference pipeline to AWS SageMaker. The hardest part is finding the correct configuration to fit your needs while re |
| 415 |  Calling the AWS SageMaker Inference endp | _____ | Also, they provide an excellent way to monitor all the HTTP errors, such as 4XX and 5XX, in one place. Figure 10.7: AWS SageMaker twin inference endpo |
| 416 |  Calling the AWS SageMaker Inference endp | _____ | aws_access_key_id=settings.AWS_ACCESS_KEY, aws_secret_access_key=settings.AWS_SECRET_KEY, ) self.endpoint_name = endpoint_name self.payload = default_ |
| 417 |  Calling the AWS SageMaker Inference endp | _____ | This method updates the inputs field of the payload with the new input text provided by the user. Additionally, it allows for modifying inference para |
| 418 |  Calling the AWS SageMaker Inference endp | _____ | Also, it accepts the query parameter, which represents the user input. Ultimately, it takes an optional context field if you want to do RAG, and you c |
| 419 |  Building the business microservice using | _____ | self.llm.set_payload( inputs=self.prompt.format(query=self.query, context=self.context), parameters={ "max_new_tokens": settings.MAX_NEW_TOKENS_INFERE |
| 420 |  Building the business microservice using | _____ | class QueryResponse(BaseModel): answer: str Now that we’ve defined our FastAPI components and have all the SageMaker elements in place, let’s reiterat |
| 421 |  Building the business microservice using | _____ | Ultimately, we define the rag_endpoint() function, used to expose our RAG logic over the internet as an HTTP endpoint. We use a Python decorator to ex |
| 422 |  Autoscaling capabilities to handle spike | _____ | This FastAPI server runs only locally. The next step would be to deploy it to AWS Elastic Ku- bernetes Service (EKS), a self-hosted version of Kuberne |
| 423 |  Autoscaling capabilities to handle spike | _____ | Previously, we configured our multi-endpoint service using the ResourceRequirements class from SageMaker. For example, let’s assume we requested four  |
| 424 |  Autoscaling capabilities to handle spike | _____ | Figure 10.8: Autoscaling possible use cases Without going into the little details of cloud networking, when working with multi-replica sys- tems, betw |
| 425 |  Registering a scalable target | _____ | All the requests first go to the ALB, which knows to route them to a replica. The ALB can adopt various routing strategies, where the simplest one is  |
| 426 |  Creating a scalable policy | _____ | Creating a scalable policy Once your scalable target is registered, the next step is defining how the scaling should occur. This is where creating a s |
| 427 |  Cooldown period | _____ | One significant advantage of setting up target tracking policies using Application Auto Scaling is that they simplify the scaling process. You no long |
| 428 |  Summary | _____ | Autoscaling is a critical component in any cloud architecture, but there are some pitfalls you should be aware of. The first and most dangerous one is |
| 429 |  References | _____ | References • AWS Developers. (2023, September 22). Machine Learning in 15: Amazon SageMaker High-Performance Inference at Low Cost [Video]. YouTube. h |

## Done when

- Every content page in 384–429 has a non-generic diagram with term eyebrow + plain title + 1–2 sentence caption (real terminology) + Inference accent + page number.
- All figures above are redrawn as clean schematics (no bitmaps); architecture figures use real tool logos as nodes.
- `CHAPTER10` is registered in `src/book.ts` and shows in the sidebar + reader.
- `npx tsc -b` and `npm run build` are clean; you screenshot the chapter and self-critique for shape consistency before moving on.
